// Typecheck-only pass for scripts/, which sits outside the build's rootDir.
{
  "extends": "../../../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "composite": false,
    "declaration": false,
    "declarationMap": false
  },
  "include": ["src/**/*", "scripts/**/*"]
}
