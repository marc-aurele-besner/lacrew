{
  "name": "@lacrew/cli",
  "version": "0.0.0",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "lacrew": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "test": "node --import tsx --test src/scaffold.test.ts",
    "clean": "rm -rf dist",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@lacrew/core": "workspace:*",
    "@lacrew/flows": "workspace:*",
    "@lacrew/orchestrator": "workspace:*",
    "@lacrew/sdk": "workspace:*",
    "viem": "^2.31.7"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  }
}
